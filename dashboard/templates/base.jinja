<!DOCTYPE html>
<html lang="en">

<head>

    <title>Missouri Wins Guide to 2022 Legislative Races</title>

    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" integrity="sha384-zCbKRCUGaJDkqS1kPbPd7TveP5iyJE0EjAuZQTgFLD2ylzuqKfdKlfG/eSrtxUkn" crossorigin="anonymous">

    <link rel=stylesheet href="{{ static('css/styles.css') }}" />
    
</head>

<body>
    <header>
        <nav class="navbar navbar-light bg-light">
            <div class="nav nav-fill justify-content-start">Missouri Wins Guide to 2022 Legislative Races</div>
            <div class="navbar-nav">
                {% if user.is_authenticated %}
                <a class="google-logo d-flex justify-content-end btn btn-outline-secondary" href="{{ url('gauth_logout') }}">Sign out</a>
                {% else %}
                <a class="google-logo d-flex justify-content-end btn btn-outline-secondary" href="{{ url('social:begin', backend='google-oauth2') }}">Sign in</a>
                {% endif %}
            </div>
        </nav>
    </header>

    <main role="main">
        <div class="container-fluid">
            {% block content %}
            {% endblock %}
        </div>
    </main>

</body>

</html>